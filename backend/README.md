# 3D Printing Backend (Express + MongoDB)

## Quick start (local)

1. Create `.env` from `.env.example` and fill values (MONGODB_URI, JWT_SECRET, OWNER_EMAIL, SMTP_*)
2. Install deps:
   ```bash
   cd backend
   npm install
